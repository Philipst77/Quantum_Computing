#include "QuantumAlgorithm.h"

// Base class has no additional implementation beyond what's in the header.
// All virtual methods are defined in subclasses.
// This file exists to satisfy the build system's compilation unit requirements.

namespace QSE {
// (intentionally empty — inline definitions in header cover everything)
} // namespace QSE